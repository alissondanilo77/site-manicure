# TODO - melhorias (JS/UX)

- [x] EmblaCarousel: adicionar autoplay com pausa no hover e respeitar `prefers-reduced-motion`

- [ ] EmblaCarousel: remover/ajustar qualquer efeito colateral caso o autoplay conflite com clique nos bullets

- [ ] Reveal ao rolar: criar componente `RevealOnScroll` com `IntersectionObserver` e animar seções ao entrar
- [ ] Aplicar `RevealOnScroll` em `page.tsx` (ou nas seções relevantes)
- [ ] Rodar `npm run lint` e `npm run build` para validar
- [ ] Testar manualmente no navegador (carrossel e animações no scroll)

